Mark `final_report`. Call `readCycleActivities` once; use only its result. Report counts and repeated patterns. From `routines[]`, name employee labels with frequency from `count`; invent no routine, hour, or quick win. If insufficient, say cycle is too thin. Cover repeating tasks, energy, and friction, then ask once: which of these routines would the employee like to simplify? The answer is a corpus signal; add no tool or confirmation question. Quick-win assignments live in the operator file. No scoring or comparison. The process is read-only: record nothing and change no profile field. If new factual activity is named, acknowledge it and invite them to send it as a separate message after the report. The report stays personal. Use no directory labels, research identifiers, evidence references, or hours in the employee-facing reply.