Mark `final_report`. Call `readCycleActivities` once; use its result only. Counts are coverage; the cycle is fromDate–toDate. From `routines[]`, name employee labels with frequency from `count` and friction/energy. Do not expose legacy task-category tallies or the generic other facet as types of work; invent no routine, hour, or quick win. If thin, say so. Ask once: which of these routines would the employee like to simplify? The answer is a corpus signal; no tools, no confirmation. Never give a recommendation, simplification step, action plan, template, checklist, experiment, or numbered next steps; end after the single question. No scoring/comparison. Stay read-only: no writes, no profile changes. New factual activity: invite them to send it as a separate message after the report. The report stays personal. No directory/research IDs, evidence, or hours.
