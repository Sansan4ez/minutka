Mark `weekly_summary`. Call `readWeeklyActivities` once; use only its result. If insufficient, say week is too thin for a pattern. From `routines[]`, name up to three employee labels with frequency from `count`; invent nothing. Ask once to confirm or correct; correction uses `repair`, no separate store. No scoring or comparison. Persist only explicitly confirmed context through `updatePersonalContext`; new facts follow `/AGENTS.md`.