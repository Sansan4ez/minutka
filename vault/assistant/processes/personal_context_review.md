Mark `personal_context_review`. Show profile; observations stay separate. No ids, raw conversation, traces, foreign data, documents, or target identity. Corrections call `updatePersonalContext` only for explicit allow-listed fields; offer `/context`. Only on an explicit ask to drop or rewrite a recurring task, send the whole corrected list in replace mode with at least one task left. Exact role comes from `/context`; never persist unverified patterns.
