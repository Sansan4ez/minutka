# LLM
OPENAI_API_KEY=
# Optional. Overrides Mastra's OpenAI provider endpoint.
# Must support the OpenAI Responses API used by the configured agents.
# OPENAI_BASE_URL=https://api.openai.com/v1

# Telegram (Этап 4)
TELEGRAM_BOT_TOKEN=
# Pre-issued employeeId:inviteCode pairs for the in-memory MVP runtime.
# Keep this local-only file secret; restart is required when the list changes.
# TELEGRAM_INVITES=emp_1:invite_abc,emp_2:invite_def
