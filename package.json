{
  "name": "minutka",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "npm run specs",
    "specs": "vitest run specs/executable --exclude='.direnv/**' --reporter=default --reporter=json --outputFile.json=docs/generated/spec-results.json",
    "serve": "tsx src/runtime/serve.ts",
    "telegram:dev": "TELEGRAM_MODE=polling tsx src/runtime/serve.ts",
    "cli": "tsx src/client/cli/main.ts",
    "db:migrate": "tsx src/infrastructure/postgres/migrate.ts",
    "db:status": "tsx src/infrastructure/postgres/migrate.ts --status",
    "pilot:knowledge-base:import": "tsx src/runtime/import-pilot-knowledge-base.ts",
    "pilot:schedules:backfill": "tsx src/runtime/backfill-default-schedules.ts",
    "company:anonymized:purge": "tsx src/runtime/purge-company-anonymized-activities.ts",
    "employee:data:delete": "tsx src/runtime/delete-employee-data.ts",
    "research:corpus": "tsx src/runtime/research-corpus.ts",
    "tenant:seed": "tsx src/runtime/seed-tenant-reference-directories.ts",
    "process:run": "tsx src/runtime/run-scheduled-process.ts",
    "specs:persistence": "node -e \"if (!process.env.TEST_DATABASE_URL || !process.env.TEST_MIGRATION_DATABASE_URL) { console.error('TEST_DATABASE_URL and TEST_MIGRATION_DATABASE_URL are required for specs:persistence'); process.exit(1) }\" && vitest run specs/persistence --exclude='.direnv/**'",
    "specs:minio": "node -e \"if (process.env.MINIO_SMOKE !== 'true') { console.error('MINIO_SMOKE=true is required for specs:minio'); process.exit(1) }\" && vitest run specs/smoke/minio-contract.spec.ts --exclude='.direnv/**'",
    "verify:persistence": "node -e \"if (!process.env.TEST_DATABASE_URL || !process.env.TEST_MIGRATION_DATABASE_URL) { console.error('TEST_DATABASE_URL and TEST_MIGRATION_DATABASE_URL are required for verify:persistence'); process.exit(1) }\" && npm run typecheck && TEST_DATABASE_URL=\"$TEST_DATABASE_URL\" MIGRATION_DATABASE_URL=\"$TEST_MIGRATION_DATABASE_URL\" npm run db:migrate && npm run specs:persistence",
    "verify": "npm run typecheck && npm run specs",
    "mastra:dev": "mastra dev"
  },
  "dependencies": {
    "@mastra/core": "^1.49.0",
    "@mastra/voice-openai": "^0.13.0",
    "commander": "^14.0.0",
    "mastra": "^1.18.0",
    "minio": "^8.0.7",
    "pg": "^8.22.0",
    "telegraf": "^4.16.3",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/pg": "^8.20.0",
    "tsx": "^4.23.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  }
}
